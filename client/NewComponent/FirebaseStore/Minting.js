import React, {useState} from "react";
import {ethers} from "ethers"
// import contractAddress and ABI

export const MintNFT = () => {
    const [wallet, setWallet] = useState('');
    const [minting, setMInting] = useS
}