import React from "react";

export const TermsOfServicePage = ({item}) => {
    return (
        <div>{item === 'terms-of-service' &&
            <div>{item} page</div>
         }   
            </div>   )
}