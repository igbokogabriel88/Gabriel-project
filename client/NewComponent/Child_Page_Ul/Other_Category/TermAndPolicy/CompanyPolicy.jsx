import React from "react";

export const CompanyPolicy = ({item}) => {
    return (
        <div>{item === 'privacy-policy' &&
            <div>{item} page
            </div>
         }
            </div>
    )
}